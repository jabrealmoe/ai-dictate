{
  "name": "dr-jira-dictate",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "deploy": "forge deploy",
    "forge-install": "forge install",
    "tunnel": "forge tunnel",
    "build": "cd static/frontend && npm install && npm run build",
    "lint": "echo 'Lint checks passed (placeholder)' && exit 0",
    "test:unit": "echo 'Unit tests passed (placeholder)' && exit 0",
    "test:integration": "echo 'Integration tests passed (placeholder)' && exit 0",
    "test:e2e": "echo 'E2E tests passed (placeholder)' && exit 0",
    "test:perf": "echo 'Performance tests passed (placeholder)' && exit 0",
    "test:smoke": "echo 'Smoke tests passed (placeholder)' && exit 0"
  },
  "dependencies": {
    "@forge/api": "^6.4.3",
    "@forge/resolver": "^1.7.1"
  },
  "devDependencies": {
    "@forge/cli": "^10.0.0",
    "eslint": "^9.0.0"
  }
}
